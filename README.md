# partypooper
destroy the shitty

# credits 
pngStd.py from https://github.com/corkami/collisions

hashclash from https://github.com/cr-marcstevens/hashclash

# requirements
python3 from `https://www.python.org/`

then you need to install pillow library `pip install pillow`

i recommend using linux as i haven't tested it on windows

if you are on windows you can download wsl from the microsoft store

# setup instructions
`git clone https://github.com/neetdev32/partypooper`

`cd partypooper`

`git clone https://github.com/cr-marcstevens/hashclash`

`./hashclash/build.sh`

wait for it to build and follow all prompts

# before running read this
you need to make sure both files have the same dimensions 

`collide.py` will do it for you although the output may not be to your liking

you also need to make sure both files are png files in "RGBA" mode (not rgb or palette mode)

it will only work with png files

after the script has finished running rename the outputted files to something more inconspicuous 

# running
run: `python collide.py good.png evil.png` (good.png and evil.png can be whatever files you want)

the script will output 2 files one ending in `-evil.png` and one ending in `-good.png`

upload the one ending in `-good.png` then wait for it to get approved

because they added stupid r9k filter we have to wait for the thread / image to get deleted

sage your thread so it gets deleted faster

once its deleted you are free to upload the photo ending in `-evil.png` and bypass the mpa

# final notes
if you need some images for `evil.png` look inside the images directory 

`good.png` can be anything you want provided the jannies will approve it

because they have approved the file the filehash is in the databases approved files table

why does this work? simple. they are using MD5 an algorithm that has been broken for nearly 30 years
